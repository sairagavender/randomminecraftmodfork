############################################################
# Root Gradle configuration (shared across subprojects)
############################################################

# JVM and Gradle behavior
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Shared mod coordinates used by :common (subprojects can override locally)
mod_version=1.3.0
maven_group=insidate.hardcoreplus

# Note:
# - Fabric-specific properties live in fabric/gradle.properties
# - NeoForge-specific properties live in neoforge/gradle.properties